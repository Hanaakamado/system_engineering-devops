#!/usr/bin/env bash
# Script that lists filenames in the current directory, showing only the part after the first dash

for file in *; do
  if [[ -f "$file" && "$file" == *-* ]]; then
    echo "${file#*-}"
  fi
done
